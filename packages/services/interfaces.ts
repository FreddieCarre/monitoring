export type EnergyConsumption = {
  Timestamp: string,
  Consumption: string
};

export type Weather = {
  Date: string,
  AverageTemperature: string, 
  AverageHumidity: string 
};

export type Timings = {
  start: string,
  stop: string
};
