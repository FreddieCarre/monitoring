import React from 'react';

describe('Page', () => {
  it('should render the HighCharts node using data retrieved by useQuery', async () => {
    /**
     * mock the query to return a test payload
     *
     * render the full Page
     *
     * intercept the graph options (spyOn HighCharts)
     *
     * assert graph options contain the test payload data
     *
     */
  });

  it('should render the Metrics node using data retrieved by useQuery', async () => {
    /**
     * mock the query to return a test payload
     *
     * render the full Page
     *
     * intercept the graph options (spyOn Metrics)
     *
     * assert graph options contain the test payload data
     *
     */
  });
});
