import React from 'react';

describe('DisplayData', () => {
  it('should request data using the ENERGY_CONSUMPTION query', async () => {

  });

  it('should pass the RangeContext data as variables to the query', async () => {

  });

  it('should poll at an interval of X seconds', async () => {

  });
});
